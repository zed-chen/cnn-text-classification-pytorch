[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["textcnn_pytorch"]

[project]
name = "textcnn_pytorch"
version = "0.0.1"
dependencies = [

  "pre-commit",
]
